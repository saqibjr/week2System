import java.awt.*;
import java.awt.geom.Point2D;
import java.util.ArrayList;

public class Solution {
    String input = "";
    ArrayList<Point2D.Float> room;
    ArrayList<ArrayList<Point2D.Float>> objectsArray;
    float current;
    float aim;
    ArrayList<ArrayList<Point>> results;
    Methods helper = new Methods();

    // int area = area(room)
    // aim = area * 0.3;
    // while(current < aim)
    // biggest element = findBuggest(objectsArray) need to delete object from the arrayList
    // Point result = ifHaveRoom(element);
    // if result != .... error value, add result to results
    // current = area(result) + current;
    // print all the content in objectsArray.
}
